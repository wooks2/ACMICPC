//dp
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main(void)
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);

	int N = 0;
	int min = 0;
	vector<int> a;
	
	cin >> N;
	a.resize(N, 0);

	for (int i = 0; i < N; i++)
		cin >> a[i];

	
	
	return 0;
}